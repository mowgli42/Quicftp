version: "3.8"

services:
  server:
    build: ./docker/caddy
    ports:
      - "8443:443"
      - "8080:80"
    healthcheck:
      test: ["CMD", "wget", "--no-check-certificate", "-q", "-O-", "https://localhost:443"]
      interval: 2s
      timeout: 5s
      retries: 10
      start_period: 5s
